ABUSEIPDB is a small command line script utility. It invokes an HTTPS Get over the Internet
to host abuseipdb.com to resolve if complaints have been made about a specific IP address. 
The script accepts a single parameter - The IP address to check:

    abuseipdb <ip-address>
    
The values returned are of the format:

    <ip-address> <name>, count=<n>
    
 where count <n> is the number of compaints received about the subject IP address and <name>
 indicates the owner (Similar to AS name).
